#include <stdio.h>

/**
 * main - prints the alphabetr in lowercase
 *
 * Return: 0 (success)
 */
int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
	{
		if (a != 'e' && a != 'q')
		{
			putchar(c);
		}
	}
	putchar('\n');
	return (0);
}
