#include <stdio.h>

/**
 * main - prints the alphabetr in lowercase
 *
 * Return: 0 (success)
 */
int main(void)
{
	char a;

	for (c = 'a'; c <= 'z'; c++)
	{
		if (c != 'e' && != 'q')
		{
			putchar(c);
		}
	}
	putchar('\n');
	return (0);
}
