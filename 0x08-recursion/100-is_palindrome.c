#include "main.h"
/**
 * is_palindrome - prints if a string is a palidrome or not
 * @s: pointer to the string
 *
 * Return: 1 if a string, 0 otherwise
 */

int is_palindrome(char *s)
{
	if (s == '\0')
		return (1);

	return (0);
}
