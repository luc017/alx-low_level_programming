#include <stdio.h>

/**
 * main - prints out the name of program
 * @argc: number of arguments
 * @argv: pointer to the array
 *
 * Return: 0 (success)
 */
int main(int argc, char **argv)
{

	printf("%s\n", argv[0]);

	return (0);
}
