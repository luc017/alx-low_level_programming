#include "main.h"

/**
 * _abs - prints the absolute value of an intenger
 * @n: the value to be analyse
 * Return: the absolute value always
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
