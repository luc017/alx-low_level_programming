#include "dog.h"
#include <stdio.h>
/**
 * new-dog - new dog's data
 * @name: name of that dog
 * @age: the age of the dog
 * @owner: name of the dog
 *
 * Return: NULL if func fails, 0 success
 */
dog_t *new_dog(char *name, float age, char *owner)
{
	if (new_dog  == NULL)
		return;

	new_dog->name = name;
	new_dog->age = age;
	new_dog-> = owner;
}
