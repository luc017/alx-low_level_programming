#include "main.h"

/**
 * _isdigit - returns if it a digit or not
 * @c: the parameter to be checked
 * Return: 1 is C is a digit or 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= 0 && c <= 9);
}
