#include "main.h"

/**
 * _isupper - prints if its an upper case or not
 * @c: the parameter to be checked
 * Return: 1 if c is uppercase and 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
